const config = {
    nodeRadius: 30, // Control node size
    linkColor: '#999', // Link color
    linkOpacity: 0.6, // Link opacity
    zoom: {
      minScale: 0.1, // Minimum zoom level
      maxScale: 5 // Maximum zoom level
    },
    nodeBorderColor: '#000', // Node border color
    nodeBorderWidth: 2, // Node border width
    textSize: '14px', // Text size for node labels
    imageBorderRadius: '50%', // Border radius for node images
    imageBorderColor: 'red', // Border color for node images
    imageSize: { width: 40, height: 40 }, // Node image size
};

export default config;
